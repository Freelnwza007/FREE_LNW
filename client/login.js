const apiBase = ["localhost", "127.0.0.1"].includes(window.location.hostname) && window.location.port !== "5000" ? "http://localhost:5000" : "";
const form = document.querySelector("#login-form"); const message = document.querySelector("#login-message"); const button = document.querySelector("#submit-button"); const nameField = document.querySelector("#name-field"); const toggle = document.querySelector("#signup-toggle"); let registerMode = false;
toggle.addEventListener("click", (event) => { event.preventDefault(); registerMode = !registerMode; nameField.classList.toggle("is-hidden", !registerMode); document.querySelector("#name").required = registerMode; document.querySelector("#panel-kicker").textContent = registerMode ? "NEW HERE?" : "WELCOME BACK"; document.querySelector("#login-title").textContent = registerMode ? "สมัครสมาชิก" : "เข้าสู่ระบบ"; document.querySelector("#panel-description").textContent = registerMode ? "สร้างบัญชีเพื่อเริ่มต้นการเดินทางของคุณ" : "ยินดีต้อนรับกลับมา เติมพลังให้วันของคุณกัน"; button.innerHTML = registerMode ? "สร้างบัญชี <span>→</span>" : "เข้าสู่ระบบ <span>→</span>"; toggle.textContent = registerMode ? "เข้าสู่ระบบ" : "สมัครสมาชิก"; message.textContent = ""; });
form.addEventListener("submit", async (event) => { event.preventDefault(); message.textContent = ""; message.className = "form-message"; const values = new FormData(form); const name = values.get("name")?.trim(); const email = values.get("email")?.trim(); const password = values.get("password"); if (!email || !password || (registerMode && !name)) { message.textContent = registerMode ? "กรุณากรอกชื่อ อีเมล และรหัสผ่าน" : "กรุณากรอกอีเมลและรหัสผ่าน"; message.classList.add("error"); return; } if (password.length < 8) { message.textContent = "รหัสผ่านต้องมีอย่างน้อย 8 ตัวอักษร"; message.classList.add("error"); return; } button.disabled = true; button.textContent = registerMode ? "กำลังสร้างบัญชี..." : "กำลังเข้าสู่ระบบ..."; try { const response = await fetch(`${apiBase}${registerMode ? "/api/auth/register" : "/api/auth/login"}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, email, password }) }); const text = await response.text(); const data = text ? JSON.parse(text) : {}; if (!response.ok) throw new Error(data.message || `ดำเนินการไม่สำเร็จ (HTTP ${response.status})`); localStorage.setItem("portAbleToken", data.token); localStorage.setItem("portAbleUser", JSON.stringify(data.user)); window.location.assign("/#account"); } catch (error) { message.textContent = error instanceof TypeError ? "เชื่อมต่อ server ไม่ได้ กรุณาเปิด backend ที่พอร์ต 5000" : error.message; message.classList.add("error"); } finally { button.disabled = false; button.innerHTML = registerMode ? "สร้างบัญชี <span>→</span>" : "เข้าสู่ระบบ <span>→</span>"; } });
